# QE Interlayer Spacing (Streamlit)
Upload a Quantum ESPRESSO `pw.x` output and get per-layer mean Z and interlayer spacing.

## Local dev
```bash
pip install -r requirements.txt
streamlit run app.py
